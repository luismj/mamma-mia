import React, { Component } from 'react';

export default class OrderComponent extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return(<div>Order Works</div>);
    }
}